const ButtonDataTable = ({ customColorBtn, onClick, children }) => {
  return (
    <button
      className={`px-2 py-1 border rounded-md hover:shadow-sm
        text-${customColorBtn}-500 border-${customColorBtn}-500 hover:shadow-${customColorBtn}-600`}
      onClick={onClick}
    >
      {children}
    </button>
  );
};

export default ButtonDataTable;
